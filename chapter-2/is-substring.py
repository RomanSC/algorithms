"""
"""
string_list = ['cat', 'bat', 'fat', 'pat', 'sat', 'hat']

substring = "PAN"
string = "ANPANMAN"

def is_substring(substring, string):
    for x in range(len(string)):
            if string[x] == substring[i]:
                return substring[i]
                #print(substring[i])

#print(is_substring(substring, string))
